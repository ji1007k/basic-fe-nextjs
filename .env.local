# Nextjs는 .env 파일 자동 로드. Express 는 dotenv로 명시적으로 로드해줘야 함
# 기본적으로 로드되는 환경변수 설정 파일

### 서버 접근용 환경변수
PORT=3000

# 프로젝트 모드
#NODE_ENV=production
NODE_ENV=development
USE_HTTPS=true

# EC2 API 서버 사용 여부
USE_REMOTE_API=false

